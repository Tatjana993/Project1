var fs = require('fs');
var path = require('path');
var myNumber = undefined;

exports.testBack = function (callback) {
  
   fs.readFile(path.join(__dirname, 'm.txt'), {encoding: 'utf-8'}, function (err, data) {
        if(err) throw err;
           // myNumber = parseInt(data);  // ovo baci izuzetak, Invalid status code
            callback(data);
        
    });
}
